# Frontend demo mode.
#
# Loaded by `npm run dev:demo` / `npm run build:demo` (vite --mode demo).
# Routes every API and auth call through src/mocks/ so the full UI runs with no
# backend, database, AI service or LLM service.
#
# Contains no secrets. Demo credentials are fixtures, not real accounts.

VITE_MOCK_API=true
